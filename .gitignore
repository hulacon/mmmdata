# macOS
.DS_Store

# Ruby / Bundler
.bundle/
vendor/

# Jekyll (root-level, in case of accidental builds)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Archives
*.zip

# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environment
.venv/
venv/

# Model weights
*.pt
*.pth

# SLURM logs and output
logs/
*.out
*.err

# Pipeline work directories
work_*/

# Local caches (atlas downloads, etc.)
.cache/

# Generated reports
validation_report.tsv

# Generated conversion tables — data, not code. They are keyed on raw source
# filenames (which embed acquisition date-times), and this repo is public.
# Regenerate: scripts/edf_triage.sbatch, then generate_inventory.py.
src/python/raw2bids_converters/edf_triage.csv
src/python/raw2bids_converters/file_inventory.csv

# Per-operator config overrides. Untracked so one operator's settings do not
# apply to everyone; config/local.toml.example is the tracked template.
config/local.toml
