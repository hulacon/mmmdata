title: MMMData Documentation
description: A documentation webpage for the MMMData project.
theme: just-the-docs

url: https://hulacon.github.io/mmmdata/

defaults:
  - scope:
      path: "doc" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
