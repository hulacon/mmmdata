[pytest]
testpaths = tests
# --strict-markers so a typo'd marker fails loudly instead of silently
# running a test that was meant to be gated.
addopts = --strict-markers
markers =
    requires_dataset: reads the real dataset on Talapas GPFS; skipped off-cluster
    integration: exercises real files/services rather than fixtures
